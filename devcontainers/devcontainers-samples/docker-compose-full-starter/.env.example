# Copy to .env for local development
# MariaDB connection settings

# Dentro Docker Compose usa il nome servizio
MARIADB_HOST=mariadb
MARIADB_PORT=3306
MARIADB_DATABASE=pizza_store

# (Opzionale) Porta sull'host a cui pubblicare MariaDB (default 3306).
# Se sull'host hai gia' un MariaDB su 3306, usa ad esempio 3307.
MARIADB_HOST_PORT=3306

# Best practice: l'app NON usa root
MARIADB_USER=pizza_user
MARIADB_PASSWORD=change-me

# (Dev / didattica) Se fai migrazioni EF da devcontainer potresti volere permessi elevati sul DB.
# 0 = least-privilege, 1 = ALL PRIVILEGES (in base allo scope)
MARIADB_GRANT_ALL_ON_DB=1

# (Dev / prod) Scope dei permessi:
# - database: grants confinati a ${MARIADB_DATABASE}.* (piu' vicino alla produzione)
# - server  : grants su *.* (comodo in sviluppo quando vuoi "fare tutto")
MARIADB_GRANT_SCOPE=server

# (Dev) Permette all'utente applicativo di fare GRANT ad altri utenti.
# In genere: 0 in produzione.
MARIADB_GRANT_WITH_GRANT_OPTION=1

# Esempio valori "prod-like":
# MARIADB_GRANT_ALL_ON_DB=0
# MARIADB_GRANT_SCOPE=database
# MARIADB_GRANT_WITH_GRANT_OPTION=0

# Password dell'utente root (serve al container MariaDB per inizializzare)
MARIADB_ROOT_PASSWORD=change-me-root

# Seed iniziale (3 record) nello script .devcontainer/db-init/02-seed.sh
# 1 = abilita seed (default), 0 = disabilita seed
MARIADB_ENABLE_SEED=1
