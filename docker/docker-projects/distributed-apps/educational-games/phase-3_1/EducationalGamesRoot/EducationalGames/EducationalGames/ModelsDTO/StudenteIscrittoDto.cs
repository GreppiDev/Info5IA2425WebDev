namespace EducationalGames.ModelsDTO;

// DTO per rappresentare uno studente iscritto (utile in futuro)
/*
public record StudenteIscrittoDto(
    int IdStudente,
    string NomeCompleto,
    string Email,
    DateTime DataIscrizione
);
*/
